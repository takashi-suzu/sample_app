<h2>タイトル</h2>
<p><%= @list.title %></p>
<h2>本文</h2>
<p><%= @list.body %></p>

<%= link_to "編集", edit_todolist_path(@list.id) %>

<p>画像</p>
<%= attachment_image_tag @list, :image, format: "jpeg", size:"300x300" %>